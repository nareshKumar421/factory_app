# order_processing

Order → warehouse check → production plan.

**Design only so far — no code.** Read [docs/DESIGN.md](docs/DESIGN.md).

Not to be confused with the existing `oms/` app, which is a narrow proxy for OMS
*invoice approval* and holds no orders.

Waiting on: OMS database credentials, and the four answers in §6 of the design.
