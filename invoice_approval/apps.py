from django.apps import AppConfig


class InvoiceApprovalConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "invoice_approval"
    verbose_name = "Invoice Approval (SAP)"
