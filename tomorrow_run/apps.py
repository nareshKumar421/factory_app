from django.apps import AppConfig


class TomorrowRunConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "tomorrow_run"
    verbose_name = "Tomorrow's run (Oil machine plan)"
