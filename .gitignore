# Environment
.env
env
.env.local
.env.live
env.copy
.venv/
*venv*

# Local Claude Code settings (machine-specific, not shared)
.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so

# Django
db.sqlite3
# `manage.py test --parallel N` clones the database per worker and drops the
# clones here, even when the test settings ask for :memory:.
default_*.sqlite3
*.log
media/

# IDE
.idea/
.vscode/
*.swp
*.swo
meta/

# OS
.DS_Store
Thumbs.db

# Database tools
*.sqbpro

# Firebase
firebase-service-account.json

# keys
private
private.pub

# Collected static files (regenerated by collectstatic)
staticfiles/

# Local working notes and one-off analysis output (kept on disk, not shared)
AMAZON_MARKETPLACE_DESIGN.md
flipkart_duplicate_orders.csv

# Local env backups — these hold live DB and SAP credentials.
.env.bak*
.env.*.bak
